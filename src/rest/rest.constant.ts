export enum AppRoute {
  Offers = '/offers',
  Offer = '/offers/:offerId',
  Users = '/users',
  Comments = '/comments',
  Favorites = '/favorites',
  Premium = '/premium/:cityName',
  Root = '/',
}

export enum StaticRoute {
  Upload = '/upload',
  Files = '/static',
  ApiDocs = '/api-docs',
}
